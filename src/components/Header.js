function Header({ name }) {
  return (
    <header className="header">
      <h1>Wilkommen zu {name}</h1>
    </header>
  );
}

// const h1styling = {
//   color: "blue",
//   textDecoration: "underline",
// };

export default Header;
